[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fem-gpe"
version = "0.1.0"
description = "Finite element solver for the Gross-Pitaevskii equation"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["fem_gpe"]